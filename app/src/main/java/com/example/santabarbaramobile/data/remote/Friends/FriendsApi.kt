// FriendsApi.kt
package com.example.santabarbaramobile.data.remote.friends

import retrofit2.Response
import retrofit2.http.GET
import retrofit2.http.Header
import retrofit2.http.POST
import retrofit2.http.Path

interface FriendsApi {

    /*
    @GET("api/friends/status/{otherAuthId}")
    suspend fun getRelationshipStatus(
        @Header("Authorization") token: String,
        @Path("otherAuthId") otherAuthId: String
    ): Response<RelationshipResponse>

    @POST("api/friends/request/{receiverAuthId}")
    suspend fun sendFriendRequest(
        @Header("Authorization") token: String,
        @Path("receiverAuthId") receiverAuthId: String
    ): Response<Unit>
*/
}